
<h1>Dashboard</h1>
  <section class="card">
    <header><h2>Add a new Blog Post</h2></header>
    <form class="new-blogpost-form">
        <div>
            <label for="new-blogpost_title">Blog Title:</label>
            <input type="text" id="new-blog_title" />
        </div>
        <div>
            <label for="new-blogpost_text">Blog Index:</label>
            <textarea id="new-blog_text" rows="10"></textarea>
        </div>
        <div>
            <button type="submit">post</button>
        </div>
    </form>
  </section>

<br />

<h2>Blog Contributions:</h2>
  {{#each blogposts}}
    <section class="card">
        <header><a href="/blogposts/{{id}}">{{blogpost_title}}</a></header>
        <p>Created by: {{user_id}}</p>
        <button class="delete-post" data-post-id="{{id}}">Delete post</button> 
    </section>
  {{/each}}

<script src="/js/add-blogpost.js"></script>
<script src="/js/delete-blogpost.js"></script>